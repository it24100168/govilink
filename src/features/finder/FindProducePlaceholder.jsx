import ProduceFinder from './ProduceFinder';

/**
 * FindProduce View
 * Retained for backwards compatibility across feature branches
 */
export default function FindProducePlaceholder(props) {
  return <ProduceFinder {...props} />;
}
