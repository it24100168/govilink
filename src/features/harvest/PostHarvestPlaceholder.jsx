import HarvestForm from './HarvestForm';

/**
 * PostHarvest View
 * Retained for backwards compatibility across feature branches
 */
export default function PostHarvestPlaceholder(props) {
  return <HarvestForm {...props} />;
}
